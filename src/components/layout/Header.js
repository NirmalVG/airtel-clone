import React from "react";
import MainNavigation from "../ui/MainNavigation";

const Header = () => {
    return (
        <header>
            <MainNavigation />
        </header>
    );
};

export default Header;
